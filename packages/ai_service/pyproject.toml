[tool.poetry]
name = "fmd-ai-service"
version = "1.0.0"
description = "FMD AI Service — LLM-based Music Direction Engine"

[tool.poetry.dependencies]
python = "^3.11"
openai = "^1.0"
anthropic = "^0.25"
pydantic = "^2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
